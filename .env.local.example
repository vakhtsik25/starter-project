# PostHog — used by the /early-access experiment landing page (lib/posthog.ts).
# Create a free project at https://posthog.com, then copy its Project API
# key and host below. Without these set, tracking silently no-ops.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
